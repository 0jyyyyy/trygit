불 값
1 : 참(true) 0 : 거짓(false) 

불 값을 표현할때는 따옴표로 감싸지 않는다.

비교 연산자를 이용하여 결과값을 보여줄수있다.

초과 : <,> 
미만 : >,<
이상 : >=
이하 : <=
같다 : ==
* = 는 대입 연산자이다.
같지않다 : !=

<참고>
NaN 끼리 비교할 때는 false 값을 가진다. 이는 숫자, 문자, 불 값을 통틀어 
false가 나오는 유일한 값
(!=를 사용했을때는 true가 나온다.)

true > false 는 true가 나온다. 1 > 0 과 같은 의미이기때문에

문자열도 비교가 가능하다. 문자값은 사전순으로 크기가 정해진다.
*특수문자도 비교가 가능하다. 이때 특수문자의 값(크기)을 알고 싶을때는
'특수문자'.charCodeAt() 으로 알 수 있다. 문자도 이 방법으로 알 수 있다.

== 와 === 의 차이
==은 값이 같은지 비교하고,
===은 값뿐만 아니라 자료형까지도 같은지 비교한다.
즉, ==은 값만 비교하기 때문에 두 값중 어떤것이 형 변환 한것인지까지는 알지못한다.

!=와 !==도 마찬가지