//퀴즈
/**
 *  다음 코드의 console.log 결과를 맞춰보세요
 * 
 * const hof = (a) => (b) => (c) => {
 *  return a + (b * c)
 * };
 * 
 * const first = hof(3);
 * const second = first(4);
 * const third = second(5);
 * console.log(third); 23?
 */